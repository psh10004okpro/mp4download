# 🔧 run.bat 실행 안될 때 문제 해결 가이드

run.bat을 실행했을 때 창이 바로 사라지는 문제를 해결하는 방법입니다.

---

## 🚨 문제 확인하기

### 방법 1: run_debug.bat 실행 (가장 쉬움!)

1. **`run_debug.bat`** 파일을 더블클릭
2. 창이 열리면서 어떤 단계에서 문제가 발생하는지 보여줍니다
3. 에러 메시지를 읽고 아래 해결 방법을 따르세요

---

### 방법 2: 명령 프롬프트에서 직접 실행

#### 단계 1: 명령 프롬프트 열기

**방법 A: 폴더에서 열기 (Windows 10/11)**
1. 프로젝트 폴더(`mp4download`)를 엽니다
2. 폴더 내 빈 공간에서 **Shift + 우클릭**
3. **"여기에 PowerShell 창 열기"** 또는 **"명령 프롬프트 열기"** 선택

**방법 B: 시작 메뉴에서**
1. **Windows 키** 누르기
2. `cmd` 입력
3. **명령 프롬프트** 실행
4. 다음 명령어 입력:
   ```
   cd C:\프로젝트경로\mp4download
   ```
   (실제 경로로 변경하세요)

#### 단계 2: Python 확인

명령 프롬프트에서 입력:
```
python --version
```

**결과:**

✅ **성공 예시:**
```
Python 3.11.5
```
→ Python이 설치되어 있습니다. 다음 단계로 진행하세요.

❌ **실패 예시:**
```
'python'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
```
→ **Python이 설치되지 않았습니다!** 아래 "Python 설치" 섹션을 보세요.

#### 단계 3: 패키지 설치

명령 프롬프트에서 입력:
```
pip install -r requirements.txt
```

또는 개별 설치:
```
pip install customtkinter yt-dlp pillow
```

#### 단계 4: 프로그램 실행

```
python video_downloader_gui.py
```

에러가 발생하면 에러 메시지를 확인하세요!

---

## 🐍 Python 설치하기

Python이 설치되지 않았다면:

### 1단계: Python 다운로드

1. **https://www.python.org/downloads/** 접속
2. 큰 노란색 **"Download Python 3.xx"** 버튼 클릭
3. 다운로드한 파일 실행 (예: `python-3.11.5-amd64.exe`)

### 2단계: Python 설치

**중요! 반드시 다음을 확인하세요:**

1. 설치 창 하단의 **"Add Python to PATH"** 체크박스 **반드시 체크!** ✅
   ```
   ☑ Add Python 3.xx to PATH
   ```

2. **"Install Now"** 클릭

3. 설치 완료될 때까지 대기 (1-2분)

4. **"Close"** 클릭

### 3단계: 설치 확인

1. **새로운** 명령 프롬프트 열기 (기존 창 닫고 다시 열기!)
2. 다음 입력:
   ```
   python --version
   ```
3. 버전이 표시되면 성공!

---

## 📦 패키지 설치 문제

### 문제 1: pip를 찾을 수 없음

```
'pip'은(는) 내부 또는 외부 명령이 아닙니다.
```

**해결:**
```
python -m pip --version
```

그래도 안 되면 Python을 재설치하고 "Add to PATH"를 체크하세요.

### 문제 2: 패키지 설치 실패

```
ERROR: Could not install packages...
```

**해결 A: pip 업그레이드**
```
python -m pip install --upgrade pip
```

**해결 B: 관리자 권한으로 실행**
1. 시작 메뉴에서 "명령 프롬프트" 검색
2. **우클릭** → **"관리자 권한으로 실행"**
3. 프로젝트 폴더로 이동:
   ```
   cd C:\프로젝트경로\mp4download
   ```
4. 패키지 설치:
   ```
   pip install -r requirements.txt
   ```

### 문제 3: customtkinter 설치 실패

수동으로 하나씩 설치:
```
pip install customtkinter
pip install yt-dlp
pip install pillow
```

---

## 🖥️ 프로그램 실행 문제

### 문제 1: ModuleNotFoundError

```
ModuleNotFoundError: No module named 'customtkinter'
```

**해결:**
```
pip install customtkinter
```

### 문제 2: tkinter 에러

```
ModuleNotFoundError: No module named '_tkinter'
```

**해결:**

Windows의 경우 tkinter가 Python과 함께 설치됩니다.
- Python을 재설치하고 "tcl/tk and IDLE" 옵션이 체크되어 있는지 확인

### 문제 3: yt-dlp 에러

```
[오류] yt-dlp가 작동하지 않습니다
```

**해결:**
```
pip install --upgrade yt-dlp
```

---

## 💡 빠른 문제 해결 체크리스트

프로그램이 실행되지 않을 때 순서대로 확인:

- [ ] **Python 설치됨?**
  ```
  python --version
  ```

- [ ] **pip 작동?**
  ```
  pip --version
  ```

- [ ] **customtkinter 설치됨?**
  ```
  pip show customtkinter
  ```

- [ ] **yt-dlp 설치됨?**
  ```
  pip show yt-dlp
  ```

- [ ] **Pillow 설치됨?**
  ```
  pip show pillow
  ```

**모두 OK라면:**
```
python video_downloader_gui.py
```

---

## 🆘 여전히 안 될 때

### 옵션 1: 가상환경 사용

```
# 가상환경 생성
python -m venv venv

# 가상환경 활성화 (Windows)
venv\Scripts\activate

# 패키지 설치
pip install -r requirements.txt

# 프로그램 실행
python video_downloader_gui.py
```

### 옵션 2: Python 완전 재설치

1. **제어판** → **프로그램 제거**
2. **Python** 찾아서 **제거**
3. **컴퓨터 재시작**
4. **Python 새로 설치** (Add to PATH 체크!)
5. 다시 시도

### 옵션 3: 간단한 테스트 버전

간단한 테스트 스크립트를 만들어 Python이 작동하는지 확인:

프로젝트 폴더에서 명령 프롬프트를 열고:
```
python -c "print('Python 작동 확인!')"
```

다음이 출력되면 Python은 정상입니다:
```
Python 작동 확인!
```

---

## 📸 스크린샷 공유

여전히 문제가 있다면 다음을 공유해주세요:

1. **`run_debug.bat` 실행 결과 스크린샷**
2. **명령 프롬프트에서 `python --version` 실행 결과**
3. **에러 메시지 전체**

---

## 🎯 요약

**가장 흔한 원인:**
1. ❌ Python이 설치되지 않음 → Python 설치
2. ❌ Python PATH가 설정되지 않음 → 재설치 시 "Add to PATH" 체크
3. ❌ 필요한 패키지가 없음 → `pip install -r requirements.txt`

**빠른 해결:**
1. ✅ `run_debug.bat` 실행하여 에러 확인
2. ✅ 명령 프롬프트에서 단계별 실행
3. ✅ Python 재설치 (Add to PATH 체크!)

---

**도움이 필요하시면 에러 메시지와 함께 문의해주세요!** 🙂
